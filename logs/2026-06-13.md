# 2026-06-13

## Queue update

- Added new queue rows for `abandon`, `reluctant`, `adequate`, `take off`, and `as well as`.
- No duplicate headwords were found in the existing queue.
- Set `type` to `word` for single words and `phrase` for short phrases.
- Set all newly added rows to `pending` before generation.

## Entry generation

- Read `prompts/entry_spec_v1.md` before generating entries.
- Generated draft entries for the first 5 pending items according to the project safety rule for unspecified counts:
  - `immaculate`
  - `abandon`
  - `reluctant`
  - `adequate`
  - `take off`
- Left `as well as` as `pending`.
- Updated generated rows in `queue/words.csv` to `draft`.

## Continued pending processing

- Read `prompts/entry_spec_v1.md` again before continuing.
- Confirmed `as well as` was the only remaining `pending` queue item.
- Generated `entries/a/as-well-as.md`.
- Updated `as well as` in `queue/words.csv` to `draft`.

## Rewrite existing drafts under semantic coverage rule

- Read `prompts/entry_spec_v1.md` and `prompts/check_spec_v1.md` after adding the new semantic coverage rule.
- Confirmed existing draft entry files before overwriting.
- Backed up the previous 6 draft entries to `backups/2026-06-13-rewrite-v2/`.
- Recreated all 6 draft entries with stronger separation of major senses:
  - `immaculate`: clean/spotless, flawless/perfect, religious or moral purity.
  - `abandon`: leave/desert, give up a plan, abandon oneself to emotion, with abandon.
  - `adequate`: sufficient for a purpose, merely acceptable.
  - `reluctant`: unwilling psychological state, reluctant role/person label.
  - `take off`: depart by air, remove clothing/items, become successful, take leave, deduct, imitate.
  - `as well as`: addition, comparison, fixed expression `as well as can be expected`.

## Index export hyperlink rule

- Updated `scripts/export_index.py` so the Excel `file` column links to existing Markdown entry files.
- Documented the Excel hyperlink behavior in `AGENTS.md` and `README.md`.

## Automatic post-generation check rule

- Updated `AGENTS.md`, `README.md`, `prompts/entry_spec_v1.md`, and `prompts/check_spec_v1.md`.
- New generation flow requires reading `prompts/check_spec_v1.md` after `scripts/validate_entry.py` passes.
- If automatic checking succeeds, entries advance to `checked` and front matter `checked: true`.
- If content concerns remain, entries move to `needs_review` with reasons in queue notes and logs.

## Applied automatic check status to existing drafts

- Ran `scripts/validate_entry.py entries`; all 6 entries passed.
- Reviewed existing entries against `prompts/check_spec_v1.md` after the semantic coverage rewrite.
- Marked the 6 existing entries as checked in front matter and `queue/words.csv`.

## Added `stuff`

- Read `prompts/entry_spec_v1.md` and `prompts/check_spec_v1.md`.
- Added `entries/s/stuff.md` with major noun and verb senses:
  - things, belongings, and materials.
  - content, subject matter, and information.
  - substances and unnamed material.
  - packing or filling something.
  - eating until full.
- Marked `stuff` as checked in front matter and `queue/words.csv`.
- Ran `scripts/validate_entry.py entries/s/stuff.md` and `scripts/validate_entry.py entries`; all entries passed.
- Regenerated `exports/dictionary_index.csv`, `exports/dictionary_index.xlsx`, and `exports/dictionary_all.md`.

## Rechecked `stuff` audit note

- Rechecked `entries/s/stuff.md` against `prompts/check_spec_v1.md`.
- Confirmed the entry has separate major noun and verb sense blocks and passes `scripts/validate_entry.py`.
- No corrective body edits were required at this recheck; status remains `checked`.
